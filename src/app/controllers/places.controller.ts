import { Request, Response, NextFunction } from "express";

export default class TagsController {

    public nearbySearch = async (request: Request, response: Response, next: NextFunction) => {

    }
}
